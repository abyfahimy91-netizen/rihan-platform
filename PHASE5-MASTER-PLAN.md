# PHASE5-MASTER-PLAN.md — برنامه جامع توسعه فاز ۵ (نسخه ۲.۰)

**تاریخ تصویب:** ۲۰۲۶-۰۸-۱۸
**وضعیت:** Approved ✅ (تصویب بنیان‌گذار و ناظر)
**فاز:** ۵ (MVP Development)
**موتور AI-VOS:** Development Planning Engine
**ابطال:** نسخه‌های قبلی برنامه (تحت تأثیر D-074)

---

## ۱. مقدمه و هدف

این سند، **نقشه راه قطعی و بدون نقص** برای توسعه ۱۴ ماژول پروژه ریهان در فاز ۵ است. هدف این برنامه، ساخت یک محصول **کامل، فاخر و درآمدزا** است که از روز اول:
1. به دایره نزدیکان خدمت کند
2. غریبه‌ها را جذب کند (سئو)
3. بدون برندسازی شخصی کار کند
4. شأن و وقار بنیان‌گذار را حفظ کند

---

## ۲. ماتریس جامع شکاف (Gap Analysis Matrix) — نسخه نهایی

> ⚠️ این ماتریس شامل **۵ نقطه کور (Blind Spots)** کشف‌شده در ممیزی ۲۰۲۶-۰۸-۱۸ است که باید در توسعه لحاظ شوند.

| ماژول | عنوان | وضعیت فعلی | وضعیت مطلوب | شکاف و اقدامات لازم (شامل نقاط کور) |
|---|---|---|---|---|
| **M14** | معماری پلاگین‌محور | ✅ ۹۰٪ کامل | پلاگین‌محور کامل + سئو فنی + **AuditLog** | پیاده‌سازی `sitemap.xml` پویا، تکمیل `EventBus`، **پیاده‌سازی جدول AuditLog (D-067/ADR-006)**. |
| **M5** | RBAC | ✅ ۱۰۰٪ کامل | کنترل دسترسی دقیق | هیچ اقدامی نیاز نیست. |
| **M10** | احراز هویت | ✅ ۱۰۰٪ کامل | Passwordless + OTP | **پیاده‌سازی Fallback برای قطعی کاوه‌نگار (ManualSms)**. |
| **M1** | کاتالوگ | ⚠️ ۴۰٪ کامل | ۱۲ بلوک روایی + سئو + **Trust Validation** | بازنویسی `ContentBlock` به ۱۲ نوع، **پیاده‌سازی چک‌لیست اعتماد (D-048) در ادمین**، **پیاده‌سازی Empty States برای نظرات**. |
| **M2** | سفارش | ⚠️ ۵۰٪ کامل | فرآیند ۳ مرحله‌ای + D-080 | بازنویسی Checkout، پیاده‌سازی D-080، اتصال مهمان. |
| **M3** | پنل خانواده | ⚠️ ۲۰٪ کامل | پنل یکپارچه + Trust Checklist | تکمیل رابط کاربری، **اجبار ادمین به تایید چک‌لیست اعتماد قبل از انتشار محصول**. |
| **M11** | پرداخت | ⚠️ ۳۰٪ کامل | کارت‌به‌کارت + رسید اختیاری | ساخت فرم پرداخت، پنل تایید ادمین، **ثبت در AuditLog**. |
| **M7** | پیگیری سفارش | ⚠️ ۴۰٪ کامل | تایم‌لاین ۵ مرحله‌ای | تکمیل تایم‌لاین، API استعلام مهمان. |
| **M4** | پنل تأمین‌کننده | ❌ ساخته نشده | پنل مستقل + گزارش مالی | ساخت کامل ماژول از صفر. |
| **M6** | مالی (Ledger) | ❌ ساخته نشده | لایه Ledger مشترک (D-075) | ساخت کامل ماژول و Ledger Layer. |
| **M8** | نظرات معتمد | ❌ ساخته نشده | نظرات + OTP سبک + **Empty State** | ساخت کامل ماژول، **پیاده‌سازی حالت «اولین نفر باشید» برای ماه اول**. |
| **M9** | فرم سرنخ | ❌ ساخته نشده | ثبت تقاضا | ساخت کامل ماژول Lead. |
| **M12** | درباره برند | ❌ ساخته نشده | داستان برند (نه شخصی) | ساخت ماژول Content Pages، **نمایش ساعات پاسخگویی (۱۱-۱۹) و شهر (تبریز)**. |
| **M13** | هویت بصری | ⚠️ ۶۰٪ کامل | تم لوکس + RTL کامل | پولیش نهایی CSS، تطبیق با تمام صفحات. |

---

## ۳. برنامه توسعه ۶ اسپرینتی (Development Roadmap)

### 🟢 اسپرینت ۱: زیرساخت روایت‌گری (M14 & M1)
*   **هدف:** تکمیل موتورهای پلاگین‌محور، سیستم ۱۲ بلوکی، و زیرساخت‌های امنیتی.
*   **تسک‌ها:**
    1. ارتقای مدل `ContentBlock` به ۱۲ نوع بلوک.
    2. **پیاده‌سازی جدول `AuditLog` در هسته (M14)**.
    3. پیاده‌سازی `sitemap.xml` پویا و `robots.txt`.
    4. رندر بلوک‌ها در صفحه محصول.
*   **خروجی:** ادمین می‌تواند با ۱۲ بلوک روایت‌گری کند. تمام رویدادهای امنیتی لاگ می‌شوند.

### 🟡 اسپرینت ۲: هسته تراکنش (M2 & M11 & M7)
*   **هدف:** ایجاد مسیر بی‌نقص خرید برای مشتری.
*   **تسک‌ها:**
    1. بازنویسی فرآیند Checkout در ۳ مرحله (D-079).
    2. پیاده‌سازی D-080 (شفافیت قیمت).
    3. ساخت فرم پرداخت کارت‌به‌کارت (D-067).
    4. تکمیل تایم‌لاین پیگیری سفارش.
*   **خروجی:** مشتری می‌تواند خرید کند و کد پیگیری بگیرد.

### 🔴 اسپرینت ۳: فرماندهی و مدیریت (M3 & M4)
*   **هدف:** ساخت ابزارهای مدیریت برای خانواده و تأمین‌کنندگان.
*   **تسک‌ها:**
    1. تکمیل **پنل خانواده (M3)**: داشبورد، مدیریت سفارشات، ویرایشگر بلوک‌محور.
    2. **پیاده‌سازی Trust Validation در M3**: ادمین باید چک‌لیست اعتماد (عکس، داستان، قیمت، ساعات) را تایید کند.
    3. ساخت **پنل تأمین‌کننده (M4)**.
*   **خروجی:** مدیریت کامل سایت توسط خانواده و تأمین‌کنندگان.

### 🟣 اسپرینت ۴: اعتماد و رشد (M8 & M9 & M12)
*   **هدف:** تبدیل بازدیدکننده غریبه به مشتری وفادار.
*   **تسک‌ها:**
    1. ساخت ماژول **نظرات معتمد (M8)** با OTP سبک و **Empty State**.
    2. ساخت ماژول **سرنخ (M9)**.
    3. ساخت ماژول **درباره برند (M12)** با داستان برند و ساعات پاسخگویی.
*   **خروجی:** قیف فروش ۵ مرحله‌ای فعال می‌شود.

### 🔵 اسپرینت ۵: لایه مالی و گزارش‌ها (M6)
*   **هدف:** شفافیت مالی و محاسبه دقیق سود.
*   **تسک‌ها:**
    1. پیاده‌سازی لایه Ledger مشترک (D-075).
    2. ساخت گزارش‌های درآمد و تسویه حساب تأمین‌کنندگان.
*   **خروجی:** گزارش‌های مالی دقیق.

### ⚪ اسپرینت ۶: یکپارچگی و درخشش (M13 & تست‌های نهایی)
*   **هدف:** اطمینان از شأن، وقار و کیفیت نهایی.
*   **تسک‌ها:**
    1. پولیش نهایی هویت بصری (M13).
    2. تست امنیتی و End-to-End.
*   **خروجی:** سایت آماده برای اولین درآمد واقعی.

---

## ۴. پروتکل اجرایی قطعی (The Audit Protocol)

هر تسک توسعه باید این ۷ مرحله را طی کند:
1. **مطالعه و جستجو (R&D):** خواندن مستندات + جستجوی اینترنتی برای متدهای روز.
2. **پیش‌نویس منطق (Drafting):** نوشتن کد.
3. **ممیزی ناظر (Audit):** تطبیق کد با اسناد (TRUST-CHECKLIST, ADRs).
4. **تولید دستور ایمن (Base64):** جلوگیری از به‌هم‌ریختگی فونت در Termius.
5. **تست محلی توسط بنیان‌گذار:** اجرای دستور و گزارش خروجی.
6. **بازخوانی از مخزن (Verification):** ناظر فایل را از گیت‌هاب بازخوانی می‌کند.
7. **کامیت و مستندسازی (Commit & Document):** ثبت در `CONTINUITY.md` و `DECISIONS.md`.

---

## ۵. ارجاعات
- docs/MVP-SCOPE.md
- decisions/D-079-RETURN-TO-ORIGINAL-VISION.md
- docs/TRUST-CHECKLIST.md
- decisions/adr/ADR-006-authentication.md
- docs/INVENTORY-FLOW.md
