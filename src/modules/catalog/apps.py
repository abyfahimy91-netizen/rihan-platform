from django.apps import AppConfig

class CatalogConfig(AppConfig):
    default_auto_field = 'django.db.models.BigAutoField'
    name = 'src.modules.catalog'
    label = 'catalog'
    verbose_name = "ماژول ۱: کاتالوگ محصول (M1)"
